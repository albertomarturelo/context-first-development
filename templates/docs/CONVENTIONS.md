<!-- Loaded on demand for "how do we do X?" questions. -->

# Conventions

## Code Style

- <e.g. "TypeScript strict mode. No `any` without an ADR.">

## Patterns

- <e.g. "Repository pattern for all data access (see ADR-001).">
- <e.g. "Dependency injection over singletons (see ADR-007).">

## Testing

- <e.g. "Integration tests use a real database, never mocks (see ADR-004).">
- <e.g. "Every public endpoint has at least one integration test.">

## Naming

- <e.g. "Files: kebab-case. Classes: PascalCase. Functions: camelCase.">

## Commits & PRs

- <e.g. "Conventional commits. PRs reference an issue or ADR.">

<!--
THIS is the file where you DOCUMENT THE AGENT'S CORRECTIONS. If you find
yourself fixing the same pattern twice, the convention belongs here. Next
session, the model reads it automatically.

Rule of thumb: a convention here is a small "law" of the codebase. ADRs
are for decisions; conventions are for consistent expressions of those
decisions.
-->
