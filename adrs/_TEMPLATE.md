<!-- Shareable ADR. Token budget: ~<estimate, e.g. 500>. -->

# ADR-`<NNN>`: `<Decision Title>`

## Status

`<Accepted | Proposed | Superseded by ADR-XXX | Deprecated>` — `<YYYY-MM-DD>`

## When to Use

Adopt this ADR when:

- `<criterion 1>`
- `<criterion 2>`

Do NOT adopt this if `<conditions where it's overkill or wrong>`.

## Context

<2–5 sentences. What problem does this ADR solve? Why does it need to be
made now? What constraints apply? Mention the typical failure mode the
ADR prevents — that is what makes the rationale stick.>

## Decision

<What is the rule? Be specific enough that a reader can verify compliance
in their own repo. Include concrete file conventions, naming patterns, or
code shapes when applicable. Be technology-neutral when possible; if a
specific stack is required, name it explicitly.>

## Alternatives Considered

1. **`<Alternative A>`** — <one-line summary and why rejected>.
2. **`<Alternative B>`** — <one-line summary and why rejected>.
3. **`<Alternative C>`** — <if applicable>.

## Verifiable Consequences

A reader can confirm this ADR is being followed if:

- `<Concrete check 1 — e.g., a grep that returns 0 results>`.
- `<Concrete check 2 — e.g., a directory layout>`.
- `<Concrete check 3 — e.g., a test that runs in isolation>`.

## Trade-offs

<What it costs to adopt this ADR. Be honest about overhead and edge
cases.>

<!--
Contract enforced (see CONTRIBUTING.md):
- ≤100 lines.
- Token budget annotation at the top.
- "When to Use" + "Verifiable Consequences" sections present.
- Technology-neutral when possible.
- English only.
-->
